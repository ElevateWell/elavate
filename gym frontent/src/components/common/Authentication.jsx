import React from "react";

const Authentication = (() => {
    return(
        <div>
            <h1>
                Verification
            </h1>
        </div>
    )
});

export default Authentication;